# The main pipeline for dlt
resources:
  pipelines:
    dlt_pipeline:
      name: dlt_pipeline
      catalog: demo
      schema: dlt_${bundle.target}
      libraries:
        - notebook:
            path: ../src/dlt_pipeline.ipynb

      configuration:
        bundle.sourcePath: ${workspace.file_path}/src
